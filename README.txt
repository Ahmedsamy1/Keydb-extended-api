Note:

Currently I am working full-time and started the task at the end of the day thinking it was a 1-hour task, but after starting I had to postpone it to the next days..So I would really appreciate it if that was taken into account when considering the total time it took to finish the task.

A highly efficient api which implements caching that adds functionality to hatchways api.

To run the application:
1) npm install
2) npm start

To run tests:
1) npm test

The api could be pinged through:
/api/ping

A request looks like this:
api/posts?tags=history,tech&sortBy=likes&direction=desc