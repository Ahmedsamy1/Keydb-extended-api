A highly efficient api which implements caching that adds functionality to hatchways api.

To run the application:
1) npm install
2) npm start

To run tests:
1) npm test

The api could be pinged through:
/api/ping

A request looks like this:
api/posts?tags=history,tech&sortBy=likes&direction=desc